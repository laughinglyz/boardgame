Steps to start my program:

1. Start a command line, cd to "Server" directory. 
Input the command "javac *.java", then input "java Myserver" to start the server.

2.Start two command lines, cd to "Client" directory. 
Input the command "javac *.java" in one command line.
Input the command "java GameClient" in both command lines to start the applications.